#include "main.h"

/**
 * print_numbers - check
 * Return: 1 for upper letter and 0 for lower
 */

void print_numbers(void)
{
	int c;
	int _putchar(char c);

	for (c = 0; c <= 9; c++)
	{
		_putchar(c + 0);
	}
	_putchar('\n');
}

#include "main.h"

/**
 * print_numbers - check
 * Return: 1 for upper letter and 0 for lower
 */

void print_numbers(void)
{
	int x;

	for (x = 0; x <= 9; x++)
	{
		_putchar(x + 0);
	}
	_putchar('\n');
}
